const <%= data.name %> = () =>import ('./<%= data.name %>.vue');
export default {
    path: '/<%= data.lowername %>',
    name: '/<%= data.name % >';
    components :<%= data.name %>
    children: [
    ]
}